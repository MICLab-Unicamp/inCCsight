# ─────────────────────────────────────────────────────────────────────────────
# Unified requirements — ROQS + CNN pipelines
# Install: pip install -r requirements.txt
#
# PyTorch: install separately with the correct CUDA/CPU variant for your machine
#   CPU only : pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
# ─────────────────────────────────────────────────────────────────────────────

# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=1.24.2
scipy>=1.9.1
pandas>=1.5.3
matplotlib>=3.7.1

# ── Medical imaging ───────────────────────────────────────────────────────────
nibabel>=5.0.1
dipy>=1.6.0
imageio>=2.27.0

# ── Image processing ──────────────────────────────────────────────────────────
scikit-image>=0.20.0
scikit-learn>=1.2.2
Pillow>=9.4.0
PyWavelets>=1.4.1
tifffile>=2023.3.21
connected-components-3d>=3.10.5

# ── Deep learning (MONAI + PyTorch Lightning) ─────────────────────────────────
# torch must be installed separately (see header)
monai>=1.1.0
pytorch-lightning>=2.0.0
torchmetrics>=0.11.4
lightning-utilities>=0.8.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
h5py>=3.8.0
networkx>=3.0
joblib>=1.2.0
lazy_loader>=0.2
threadpoolctl>=3.1.0
packaging>=23.0
python-dateutil>=2.8.2
pytz>=2023.3
six>=1.16.0
sympy>=1.11.1
mpmath>=1.3.0
PyYAML>=6.0
fsspec>=2023.3.0
filelock>=3.10.7
